version: '3.8'

services:
  growin-api-green:
    image: ghcr.io/growin-2025/growin-server:latest
    container_name: growin-api-green
    ports:
      - "8081:8080"
    env_file:
      - .env
    networks:
      - app-network

networks:
  app-network:
    external: true