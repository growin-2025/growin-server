services:
  growin-api-blue:
    image: ghcr.io/growin-2025/growin-server:latest
    container_name: growin-api-blue
    ports:
      - "8080:8080"
    env_file:
      - .env
    networks:
      - growin-network

networks:
  growin-network:
    external: true