spring:
  config:
    activate:
      on-profile: "test"

# h2 사용하기?
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: ${TEST_DB_URL}
#    username: ${TEST_DB_USERNAME}
#    password: ${TEST_DB_PASSWORD}